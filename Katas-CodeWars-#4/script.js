function makeNegative(num) {

    let NegativeResult;

// Wenn meine Num größer 0 ist.
// Dann mach sie negative.
// Ansonsten returne mir num.

if(num > 0) {
NegativeResult = -num;
} else {
    NegativeResult = num;
}

return NegativeResult;
  
}



  
